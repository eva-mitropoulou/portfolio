# Data Note

The full working project used public ChEMBL EGFR IC50 records and generated raw/processed descriptor tables. This portfolio copy intentionally includes selected result tables, summaries, figures, and source scripts rather than large intermediate datasets. The workflow can be regenerated from the scripts in `code/` using the dependencies listed in `requirements.txt`.
